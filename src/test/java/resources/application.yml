# Profile Test
"%test":
  quarkus:
    datasource:
      db-kind: h2
      username: sa
      password: sa
      jdbc:
        url: jdbc:h2:mem:quarkus_social
      max-size: 16

    hibernate-orm:
      database:
        generation: drop-and-create
      log:
        sql: true

    log:
      level: INFO